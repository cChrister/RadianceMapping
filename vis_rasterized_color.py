import numpy as np
import matplotlib.pyplot as plt

my_dpi = 96

# img = np.load("cat.npy") # [800, 800, 3]


# threshold = 1e-4  # 设置阈值，根据实际情况调整
# img[img < threshold] = 1.0


# h, w, channels = img.shape

# plt.figure(figsize=(h/my_dpi, w/my_dpi), dpi=my_dpi)
# plt.imshow(img)
# plt.axis('off')
# plt.margins(0,0)
# plt.savefig("cat.png", bbox_inches="tight", pad_inches=0.0)
# plt.show()